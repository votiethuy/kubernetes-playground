apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: face-detector
  namespace: redaction
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 50%
  template:
    metadata:
      labels:
        app: face-detector
    spec:
      containers:
      - name: face-detector
        image: docker.taservs.net/vision/face_detection_service:20190911.60
        imagePullPolicy: Always
        ports:
        - name: grpc-face-api
          containerPort: 50053
        readinessProbe:
          exec:
            command: ["/bin/grpc_health_probe", "-addr=:50053", "-service=Inferencer"]
          initialDelaySeconds: 5
        livenessProbe:
          exec:
            command: ["/bin/grpc_health_probe", "-addr=:50053", "-service=Inferencer"]
          initialDelaySeconds: 10
        resources:
          requests:
            cpu: "2"
            memory: "16Gi"
            nvidia.com/gpu: 1
          limits:
            cpu: "2"
            memory: "16Gi"
            nvidia.com/gpu: 1
        command: ["sh","/opt/face/face-entrypoint.sh"]
        env:
        - name: SERVICE_NAME
          value: FACE_DETECTOR_SERVICE
        - name: DD_HOST
          value: "datadog.kube-system.svc.cluster.local"
        - name: CONFIDENCE_THRES
          value: "0.85"
        - name: PORT
          value: "50053"
        - name: LOG_LEVEL_CONFIG
          value: "INFO"
        - name: SENTRY_PATH
          value: "https://d3e888e477854fbabee69ccc6d5a8819:4e0d676fd9f6441c98c461c9ce4cf0e7@qag1ge1lsen001.ag1.taservs.net/2"
---
apiVersion: v1
kind: Service
metadata:
  name: face-detector
  namespace: redaction
spec:
  type: ClusterIP
  ports:
    - name: grpc-face-api
      port: 9080
      protocol: TCP
      targetPort: grpc-face-api
  selector:
    app: face-detector